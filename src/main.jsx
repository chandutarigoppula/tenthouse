import React from "react";
import ReactDOM from "react-dom/client";
import SriVenkateshwaraCatering from "./App.jsx";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <SriVenkateshwaraCatering />
  </React.StrictMode>
);
